services:
  api:
    build:
      context: .
      target: builder
    container_name: fastapi-application-prod
    environment:
      PORT: 8000
    ports:
      - '${APP_EXT_PORT:-11000}:8000'
    restart: "no"
